If you were taylor otwell creating a new set of laravel illuminate support components - each as its own crate within our @crates directory in order to implement what is needed based on our @.cursorrules technical product requirements to reach feature parity with @certmagic but in rust - how would you go about using macros like schemars, thiserror, validators, builders, etc... alongside serde for serialization and deserialzation of all input and output to provide a beatified taylor otwell inspired set of contracts at the boundary in which we will be able to use the macros and serialization/deserialization as a way to decouple the end user from needing to depend on using rust itself by providing an api layer while doing so in a way where we'll easily be able to utilize each one of our feature requirement capabilities within the implementation layer so that we are able to provide the best of both worlds.