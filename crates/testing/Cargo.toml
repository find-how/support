[package]
name = "testing"
version = "0.1.0"
edition = "2021"

[dependencies]
store = { path = "../store" }
settings = { path = "../settings" }
encrypt = { path = "../encrypt" }
tokio = { version = "1.0", features = ["full"] }
tempfile = "3.8"
assert_fs = "1.0"
predicates = "3.0"
